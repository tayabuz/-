<Page
    x:Class="Экзамен_ПДД.Settings"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Экзамен_ПДД"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <StackPanel>
        <TextBlock FontFamily="Segoe UI" Width="Auto" MinWidth="400" HorizontalAlignment="Stretch">
            <LineBreak/>
            <Run Foreground="Gray" FontFamily="Segoe UI Light" FontSize="24">
                Настройки
            </Run>
            <LineBreak/>
            </TextBlock>
            <TextBlock Text="Удаление сохранненых билетов" FontSize="18" FontFamily="Segoe UI"/>
            <Button x:Name="DeleteTickets_Button" Content="Удалить" FontSize="16" FontFamily="Segoe UI" Click="DeleteTickets_Button_OnClick"/>
        </StackPanel>
    </Grid>
</Page>
